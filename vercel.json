{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "cd server && npm install && cd ../client && corepack enable && pnpm install",
  "buildCommand": "cd server && npx prisma generate && npm run build && cd ../client && npm run build",
  "outputDirectory": "client/dist",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ],
  "functions": {
    "api/index.ts": {
      "maxDuration": 60,
      "memory": 1024
    }
  }
}
